"""Printed vs handwritten classification.

The implementation lives in ``hw_printed.py`` (copied from the existing
ConvNeXt-Tiny production classifier). Use ``existing_classifier_adapter``
from the pipeline — do not call a new algorithm.
"""
